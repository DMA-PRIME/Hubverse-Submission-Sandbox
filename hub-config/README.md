# Teaching configuration

These files make the repository look and feel like a Hubverse repository. The two example rounds demonstrate forecast and scenario concepts in one teaching space.

The custom, dependency-free checker in `scripts/validate_submission.py` is the validator actually run by this blueprint. The configuration is intentionally small and is not a substitute for an operational hub's active `tasks.json`, metadata schema, or admin settings.
